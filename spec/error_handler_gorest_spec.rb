RSpec.describe ErrorHandlerGorest do
  it "has a version number" do
    expect(ErrorHandlerGorest::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
